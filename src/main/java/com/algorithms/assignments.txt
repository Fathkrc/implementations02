1. junit-jupiter - it's junit5, not 4
OK
2. test-related dependencies must be scope=test
OK
3. final fields
ASK AGAIN!! tried to make "size" final but I cant reassign
4. Use T in DynamicArray, otherwise it's hard to compare it with Linked List
OK
5. Fix the package names in tests
OK
6. arrayCapacity() shouldn't be part of the public API
ok
7. Tests must be simple, at least the basic ones
 almost **
8. Rename tests
OK
9. Check boundary conditions
almost
10. Test Iterator.remove()
OK
11. Try running a test with coverage
OK all covered
12. Compare lists using https://github.com/openjdk/jmh
still checking documentation

#####
xor or temp variable?
(x^y) ^ y= x

*C = arr[0]

sorted array merge
long cmp = (*A <= *B); // bool
long min = *B ^ ((*B ^ *A) & (- cm)) ;
*C++ = min;
A +=cmp;
 na -= cmp;
В += !cmp;
 nb -= ! стр;

 MOD
 r=(x+y)%n

 z= x+y
 r=(z<n) ? z : z-n

 z = x+y
 r = z - ( n & -( z >= n ))

 debruijn sequence ?

 000, 001, 010, 101, 011, 111, 110, 100

